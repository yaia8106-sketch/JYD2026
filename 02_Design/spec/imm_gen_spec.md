# 立即数生成器模块规格

> 纯组合逻辑模块。根据 `imm_type` 从 32-bit 指令中提取并符号扩展立即数。

---

## 1. 端口列表

| 信号名 | 方向 | 位宽 | 类型 | 含义 |
|--------|------|------|------|------|
| `inst` | input | 32 | 数据 | 原始指令（来自 IF/ID 寄存器 `id_inst`） |
| `imm_type` | input | 3 | 控制 | 立即数类型 |
| `imm` | output | 32 | 数据（组合） | 符号扩展后的 32-bit 立即数 |

---

## 2. 功能描述

| `imm_type` | 编码 | 格式 | 生成方式 |
|------------|------|------|----------|
| `IMM_I` | `3'b000` | I-type | `{{20{inst[31]}}, inst[31:20]}` |
| `IMM_S` | `3'b001` | S-type | `{{20{inst[31]}}, inst[31:25], inst[11:7]}` |
| `IMM_B` | `3'b010` | B-type | `{{19{inst[31]}}, inst[31], inst[7], inst[30:25], inst[11:8], 1'b0}` |
| `IMM_U` | `3'b011` | U-type | `{inst[31:12], 12'b0}` |
| `IMM_J` | `3'b100` | J-type | `{{11{inst[31]}}, inst[31], inst[19:12], inst[20], inst[30:21], 1'b0}` |
| 其他 | `3'b101`~`3'b111` | — | `32'd0`（默认值，防止 latch） |

---

## 3. 时序约束

- 纯组合逻辑，无时钟、无寄存器
- `imm_type` 仅在 ID 级使用（不传入后续流水线），见 `isa_encoding.md` §1.2

---

## 4. 边界条件

- 无时钟、无复位、无 stall/flush
- 所有 `imm_type` 值必须有确定输出

---

## 5. 依赖文档

- `design_rules/isa_encoding.md` §4（立即数类型与生成规则）
